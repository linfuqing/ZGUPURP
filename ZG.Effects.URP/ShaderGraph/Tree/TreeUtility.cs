using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace ZG
{
    /*public static class TreeUtility
    {
        public static float DistanceTo(
            int index, 
            Vector3 target, 
            Vector3[] vertices, 
            int[] indices, 
            Dictionary<int, float> distances)
        {
            ref readonly var vertex = ref vertices[index];

            float distance = Vector3.Distance(vertex, target);
            int x, y, z, numIndices = indices.Length;
            for(int i = 0; i < numIndices; i += 3)
            {
                x = indices[i + 0];
                y = indices[i + 1];
                z = indices[i + 2];

                if(x == index)
                {

                }
            }
        }
    }*/
}